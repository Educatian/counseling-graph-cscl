# Cloudflare Pages local-dev config (used by `wrangler pages dev`).
# Production settings are mirrored in the Cloudflare dashboard for the Pages
# project — keep these two in sync.
name = "counseling-graph-cscl"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "dist"

# Env vars for `wrangler pages dev` only — production env vars live in the
# Cloudflare Pages dashboard. Use `wrangler pages dev --env-file=.env.local`
# for local Functions testing, or set them via `dotenv -e .env.local --` prefix.
